.. image:: ../_static/openl2m_logo.png

===========
Version 4.2
===========

**Version 4.2 highlights:**

- v4.2 now supports Python 3.14. Ie. Ubuntu 26.04 is supported!


New Configuration Options
-------------------------

.. code-block:: Python

    # Globally enable or disable reading of hardware details of all devices.
    # Disable to save read time, but this will not show stacked chassis info, etc.
    # Note: in the admin interface you can also disabled this
    # per Switch Group, or for individual devices (Switches)
    READ_HARDWARE_DETAILS = True

    # Warn about small browser sizes. We prefer a browser size of 1200 pixels. On < 568, we will warn, on the login and menu windows:
    # BROWSER_SIZE_WARNING = "WARNING: OpenL2M does not render well on small browser sizes!<br />Please use a large browser window."

    # DNS read timeout in seconds, for both forward and reverse lookups.
    DNS_TIMEOUT = 3

Version 4.2.5
-------------

Release Date: TBD

* add REST endpoint to edit interface 802.1q vlan tags


Version 4.2.4
-------------

Release Date: 2026-08-11

* allow change of interface vlan and description with a single submit.
* increase the size of search results box before scrolling is required.

Bug fixes:

* added new ruff v0.16 code compliance updates.
* fix socket timeout introduced from DNS_TIMEOUT setting.
* fix HPE Comware REST API driver idle timeout.


Version 4.2.3
-------------

Release Date: 2026-07-28

* implement PoE health checks for all drivers.
* change how health checks are called in drivers.
* add very basic Caddyserver installation steps.
* update configuration documention.
* add DNS_TIMEOUT setting.

Bug fixes:

* upgrade Gunicorn>=26.0.0 to support newer Python versions.


Version 4.2.2
-------------

Release Date: 2026-07-21

* properly render login, logo and nav bar on small browser sizes.
* add small browser size warnings on login and menu screens.
* for Aruba AOS-CX REST driver, add reading of PoE power supply and chassis info
* upgrade to FontAwesome v7.3.1

Bug fixes:

* change Neighbor Graph icon to 'chart diagram'.


Version 4.2.1
-------------

Release Date: 2026-07-14

* Add ability to Download, or Copy-to-Clipboard the output of commands run on a switch.
  Contributed by Jisnu (https://github.com/ZEUSGMJ)
* improved handling of "401 unauthorized" errors on Arista eAPI devices.
* change SNMP no-instance returns to log warning instead of error.

Bug fixes:

* fix disable hardware read functionality on HPE Comware REST driver.
* on HPE Comware REST driver, move hardware related calls into proper function to allow disable hardware read.


Version 4.2
-----------

Release Date: 2026-07-10

* add support for Python 3.14 (and thus Ubuntu 26.04). We now use the Python EzSnmp v2.3 library (from v1.1)
* add ability to disable reading of hardware info per device or per group from the admin pages, or globally via a *settings.py* variable.
  Disabling this can save significant time on older, slower devices and switch stacks.

Bug fixes:

* fix vlan change regression bug in several SNMP drivers.
* fix port to ifindex map regression bug in Cisco SNMP driver.
* fix allowed vlan parsing error in Arista eAPI driver.
* interface and ethernet spreadsheet download optimization.
* handle poe change, vlan change, and tags editing in the "standard" way in the actions.py classes.
* protect vlan actions for invalid http methods
* detect invalid data types in snmp set.
* update consistent naming of snmp _parse_mibs_functions()
* many pylint updates.